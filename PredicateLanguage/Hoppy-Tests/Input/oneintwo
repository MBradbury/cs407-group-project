[all]
function 0 as addr returning int in
	using Neighbours(1) at 255 as onehopn in
	using Neighbours(2) at 254 as twohopn in
    	@(a : onehopn ~
    	    #(b : twohopn ~ addr(a) == addr(b))
    	)

